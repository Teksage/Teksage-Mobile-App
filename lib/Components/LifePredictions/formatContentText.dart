import 'package:flutter/material.dart';

class FormatContentText extends StatelessWidget {
  final String text;
  const FormatContentText({super.key, required this.text});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
